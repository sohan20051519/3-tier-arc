# Frontend Environment Variables
# In production (Nginx reverse proxy routing /api to Backend EC2): leave empty or unset for relative /api
# In local standalone development without proxy: http://localhost:5000
VITE_API_URL=
